box: ruby:2.2.4
build:
  steps:
    - script:
      name: print some useful information about the wercker box
      code: |-
        env
    - script:
      name: run test script
      code: ruby test.rb

